module.exports = {
  PORT: process.env.PORT || 8080,
  FRONTEND_URL: process.env.FRONTEND_URL || 'http://localhost:3000',
  DEFAULT_COMMISSION_RATE: parseFloat(process.env.DEFAULT_AFFILIATE_COMMISSION_RATE || '0.05'),
  WELCOME_DISCOUNT_CODE: process.env.WELCOME_DISCOUNT_CODE || 'WELCOME10',
  AI_TIMEOUT_MS: 20000,
  MAX_UPLOAD_SIZE_BYTES: 5 * 1024 * 1024, // 5MB
  SHOPIFY_API_VERSION: '2026-07',
  RATE_LIMITS: {
    DEFAULT_WINDOW_MS: 15 * 60 * 1000, // 15 mins
    DEFAULT_MAX: 100,
    EXPENSIVE_WINDOW_MS: 15 * 60 * 1000,
    EXPENSIVE_MAX: 20
  }
};
